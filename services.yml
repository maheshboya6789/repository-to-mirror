apiVersion: v1
kind: Service
metadata:
   name: vproapp-servic3
spec:
   ports:
   - port: 30003
     nodePort: 30003
     targetPort: newport4
     protocol: TCP
  selector:
     app: newpod4
  type: NodePort
