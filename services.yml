apiVersion: v1
kind: Service
metadata:
   name: vproapp-servic6
spec:
   ports:
   - port: 30001
     nodePort: 30001
     targetPort: newport1
     protocol: TCP
  selector:
     app: newpod1
  type: NodePort
